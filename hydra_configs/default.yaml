# config.yaml

defaults:  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
hydra:  
  output_subdir: null  
  run:  
    dir: .
config_filename: null
data_dir: null
data_filename: null
data_ext: ".mp4"
landmarks_dir: null
landmarks_filename: null
landmarks_ext: ".pkl"
labels_filename: null
detector: retinaface
# LM Studio config (OpenAI-compatible API)
llm_base_url: http://localhost:1234/v1
llm_model: local  # Use your loaded model's name from LM Studio (e.g. "qwen2.5-3b" or "local")
dst_filename: null
gpu_idx: 0
output_subdir: null
